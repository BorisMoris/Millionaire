<UserControl x:Class="Millionaire.Views.EndOfGameButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Millionaire.Views"
             xmlns:models="clr-namespace:Millionaire.Models"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Content="← Zpět do menu" x:Name="backToMenuButton"
                Margin="0,0,5,0"
                Grid.Column="0" Click="backToMenuButton_Click"/>
        <Button Content="Uložit skóre" x:Name="saveScoreButton"
                Margin="5,0,5,0"
                Grid.Column="1" Click="saveScoreButton_Click"/>
        <Button Content="Nová hra →" x:Name="newGameButton"
                Margin="5,0,0,0"
                Grid.Column="2" Click="newGameButton_Click"/>
    </Grid>
</UserControl>
